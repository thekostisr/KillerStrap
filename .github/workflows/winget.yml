name: Publish to Winget

on:
  release:
    types: [released]

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: KillerStrap.KillerStrap
          token: ${{ secrets.WINGET_TOKEN }}
